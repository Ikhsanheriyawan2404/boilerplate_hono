version: '3'
services:

  hono-prod:
    image: registry.neidra.my.id/hono:latest
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - ./.env.production
    depends_on:
      - db_hono
    networks:
      - hono_network

volumes:
  db-hono:
